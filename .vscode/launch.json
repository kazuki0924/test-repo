{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Scripts",
      "type": "debugpy",
      "request": "launch",
      "console": "integratedTerminal",
      "program": "${file}",
      "env": {
        "PYTHONPATH": "${PYTHONPATH}:${workspaceFolder}"
      }
    }
  ]
}
